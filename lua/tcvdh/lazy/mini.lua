return {
    "echasnovski/mini.nvim",
    config = function()
        require('mini.animate').setup({
            -- cursor = {
            --      enable = true,
            --      speed = 10,
            --      delay = 0.1,
            --  },
            --  scroll = {
            --      enable = true,
            --      speed = 10,
            --      delay = 0.1,
            --  },
        })
    end
}

